<% for (let i = 0; i < (typeof rows === 'number' ? rows : 4); i++) { %>
<tr class="skeleton-row">
  <% for (let c = 0; c < cols; c++) { %>
  <td><span class="skeleton-sheen"></span></td>
  <% } %>
</tr>
<% } %>